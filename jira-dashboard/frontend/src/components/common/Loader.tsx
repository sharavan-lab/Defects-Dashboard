import React from 'react';
import './Loader.css'; // Create this file

const Loader = () => (
  <div className="loader-container">
    <div className="loader-spinner"></div>
    <p>Loading issues...</p>
  </div>
);

export default Loader;
